node_modules/
.env
data/
*.log

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
# Real tfvars + secrets are environment-specific / sensitive — never commit.
# (the *.example files ARE committed)
infra/terraform.tfvars
infra/*.secrets.json
